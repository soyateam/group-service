import config from '../config';
const getTokenCreator = require('spike-get-token');


export class SpikeService {
    static getToken(id: string, token?: string) {
        // return getTokenCreator(config.spike.options);
    }
}
